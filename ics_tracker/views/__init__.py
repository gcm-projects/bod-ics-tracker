"""Page views for the multi-page app, rendered via st.navigation.

Each module exposes a `render()` that draws one page. The app shell (ui.run)
handles page config, the auth gate and navigation, so pages only render content.
"""
